# buf.yaml - Buf configuration for RLSify
version: v2
modules:
  - path: proto
    name: buf.build/speajus/rlsify

lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX

breaking:
  use:
    - FILE

deps:
  - buf.build/googleapis/googleapis

